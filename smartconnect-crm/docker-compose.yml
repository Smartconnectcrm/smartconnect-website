version: '3'

services:
  payload:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartconnect-crm-payload-1
    restart: always
    ports:
      - '3000:3000'
    depends_on:
      - mongo
    env_file:
      - .env

  mongo:
    image: mongo:latest
    restart: always
    ports:
      - '27017:27017'
    command:
      - --storageEngine=wiredTiger
    volumes:
      - data:/data/db
    logging:
      driver: none

volumes:
  data: